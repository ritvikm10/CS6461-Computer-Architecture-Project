package src;

/**
 * Parses assembly instruction strings into Instruction objects.
 * 
 * Tasks:
 * - Split a line into opcode + operands.
 * - Handle labels (check SymbolTable).
 * - Convert registers, IX, I, and addresses into integers.
 * - Return an Instruction object for PassTwo.
 */
public class InstructionParser {
    public Instruction parseLine(String line, SymbolTable symTable) {
        // TODO: Implement parsing logic
        return null;
    }
}
