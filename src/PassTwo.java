package src;

/**
 * PassTwo of the assembler.
 * 
 * Tasks:
 * - Read source.asm again.
 * - Parse each instruction into fields (opcode, registers, addressing mode, address).
 * - Use OpcodeTable to map mnemonic → binary opcode.
 * - Use SymbolTable for resolving label addresses.
 * - Convert 16-bit binary instruction → octal.
 * - Call OutputWriter to write results into loadfile.txt.
 */
public class PassTwo {
    public void secondPass(String inputFile, String outputFile, SymbolTable symTable) {
        // TODO: Implement second pass logic
    }
}